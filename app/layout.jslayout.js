export const metadata = {
  title: "Prototype Practice",
  description: "Lightweight prototype practice",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body style={{ margin: 0, padding: 0 }}>{children}</body>
    </html>
  );
}
